import { StyleSheet } from 'react-native';

export const GRID_DEFAULT_GAP = 8;

export const styles = StyleSheet.create({
  rowContainer: {
    flexDirection: 'row',
    alignItems: 'flex-start',
  },
  flex: {
    flex: 1,
  },
});
