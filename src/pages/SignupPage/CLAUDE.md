# signup page

## 1. 페이지 설명

사용자가 이메일, 비밀번호, 닉네임을 입력하여 계정을 생성하는 페이지.

회원가입 완료 시 로그인 상태로 전환하지 않고,
로그인 페이지로 이동하여 로그인하도록 유도한다. (MVP 기준)

## 2. 진입 경로

- 로그인 페이지에서 "회원가입" 버튼 클릭

## 3. 이탈 경로

| 상황          | 이동          |
| ------------- | ------------- |
| 회원가입 성공 | 로그인 페이지 |
| 뒤로가기      | 로그인 페이지 |
| 회원가입 취소 | 로그인 페이지 |

## 4. 화면 구성

| UI 요소                    | 설명                       | 필수 여부 | 비고                                                                                              |
| -------------------------- | -------------------------- | --------- | ------------------------------------------------------------------------------------------------- |
| 이메일 입력창              | 이메일 입력                | Y         | placeholder: "이메일을 입력해주세요."                                                             |
| 이메일 인증 버튼           | 이메일 인증하는 버튼       | Y         | 누르면 재전송으로 문구 변경<br>10초 제한                                                          |
| 이메일 인증 코드 입력창    | 이메일 인증 코드 입력창    | Y         | placeholder: "이메일에 발송된 코드를 입력해주세요."                                               |
| 이메일 인증 코드 확인 버튼 | 이메일 인증 코드 확인 버튼 | Y         | 3분 제한                                                                                          |
| 비밀번호 입력창            | 비밀번호 입력              | Y         | secureTextEntry 적용<br>placeholder: "영문자(대,소문자), 숫자, 특수문자를 포함하여 최소 8자 이상" |
| 비밀번호 확인 입력창       | 비밀번호 재입력            | Y         | secureTextEntry 적용<br>placeholder: "비밀번호를 다시 입력해주세요."                              |
| 비밀번호 노출 버튼         | 비밀번호 노출 버튼         | Y         | 비밀번호 입력창, 비밀번호 확인 입력창 각각 하나씩                                                 |
| 닉네임 입력창              | 사용자 닉네임 입력         | Y         | placeholder: "특수문자 제외, 최소 2자 이상, 최대 12자 이하"                                       |
| 닉네임 중복 확인 버튼      | 닉네임 중복 확인 버튼      | Y         |                                                                                                   |
| 회원가입 버튼              | 회원가입 요청              | Y         | 조건 만족 시 활성화                                                                               |
| 뒤로가기 버튼              | 로그인 페이지 이동         | Y         |                                                                                                   |
| 에러 메시지 영역           | 실패 시 메시지 표시        | Y         | 상단                                                                                              |

## 5. 사용자 시나리오

1. 사용자가 이메일을 입력한다.
2. 사용자가 이메일 인증 버튼을 클릭한다.
3. 입력한 이메일로 인증 코드가 발송된다.
4. 이메일 인증 버튼 문구가 재전송으로 변경된다.
5. 이메일 인증 버튼은 10초 동안 비활성화된다.
6. 사용자가 이메일에 발송된 인증 코드를 입력한다.
7. 이메일 인증 코드 확인 버튼을 클릭한다.
8. 인증 코드 검증을 수행한다.
9. 사용자가 비밀번호, 비밀번호 확인, 닉네임을 입력한다.
10. 사용자가 닉네임 중복 확인 버튼을 클릭한다.
11. 입력값 검증 수행
    - 이메일 형식 확인
    - 이메일 인증 완료 여부 확인
    - 비밀번호 조건 확인
    - 비밀번호 일치 여부 확인
    - 닉네임 조건 확인
    - 닉네임 중복 확인 여부 확인
12. 회원가입 버튼을 클릭한다.
13. 회원가입 API 요청 수행
14. 회원가입 성공 시 로그인 페이지로 이동한다.
15. 회원가입 실패 시 에러 메시지를 표시한다.

## 6. 기능 명세

| 기능                    | 동작                           | 성공 시               | 실패 시                |
| ----------------------- | ------------------------------ | --------------------- | ---------------------- |
| 이메일 인증 코드 발송   | 입력한 이메일로 인증 코드 발송 | 인증 코드 입력 가능   | 에러 메시지 표시       |
| 이메일 인증 코드 재전송 | 인증 코드 재발송               | 재전송 완료           | 에러 메시지 표시       |
| 이메일 인증 코드 확인   | 입력한 인증 코드 검증          | 이메일 인증 완료 처리 | 에러 메시지 표시       |
| 닉네임 중복 확인        | 닉네임 사용 가능 여부 확인     | 사용 가능 표시        | 중복 메시지 표시       |
| 회원가입                | 입력값으로 회원가입 요청       | 로그인 페이지 이동    | 에러 메시지 표시       |
| 뒤로가기                | 로그인 페이지 이동             | 로그인 페이지         | -                      |
| 입력 검증               | 입력값 유효성 체크             | 회원가입 버튼 활성화  | 회원가입 버튼 비활성화 |

## 7. 입력값 검증

| 필드             | 필수 | 조건                                                          | 에러 문구                                                                        |
| ---------------- | ---- | ------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 이메일           | Y    | 이메일 형식                                                   | 올바른 이메일 형식을 입력해주세요.                                               |
| 이메일 인증      | Y    | 이메일 인증 완료                                              | 이메일 인증을 완료해주세요.                                                      |
| 이메일 인증 코드 | Y    | 3분 이내 입력                                                 | 인증 코드가 만료되었습니다. 다시 인증해주세요.                                   |
| 이메일 인증 코드 | Y    | 인증 코드 틀렸을때                                            | 인증 코드가 일치하지 않습니다. 다시 확인해주세요.                                |
| 비밀번호         | Y    | 영문 대문자, 영문 소문자, 숫자, 특수문자 포함 / 최소 8자 이상 | 비밀번호는 영문 대문자, 소문자, 숫자, 특수문자를 포함하여 8자 이상 입력해주세요. |
| 비밀번호 확인    | Y    | 비밀번호와 동일                                               | 비밀번호가 일치하지 않습니다.                                                    |
| 닉네임           | Y    | 특수문자 제외 / 2자 이상 12자 이하                            | 닉네임은 특수문자 없이 2자 이상 12자 이하로 입력해주세요.                        |
| 닉네임 중복 확인 | Y    | 중복 확인 완료<br>slang 확인                                  | 닉네임 중복 확인을 진행해주세요.                                                 |

버튼 활성화 조건

- 이메일 형식 valid
- 이메일 인증 완료
- 비밀번호 조건 충족
- 비밀번호 확인 일치
- 닉네임 조건 충족
- 닉네임 중복 확인 완료

## 8. API 명세

### 1) 이메일 인증 코드 발송

| 항목     | 내용                              |
| -------- | --------------------------------- |
| API 목적 | 이메일 인증 코드 발송             |
| Method   | POST                              |
| Endpoint | /api/auth/email/verification-code |

**Request**

```json
{
  "email": "string"
}
```

**Response 성공**

```json
{
  "success": true
}
```

**Response 실패**

```json
{
  "message": "이미 가입된 이메일입니다."
}
```

### 2) 이메일 인증 코드 확인

| 항목     | 내용                                      |
| -------- | ----------------------------------------- |
| API 목적 | 이메일 인증 코드 검증                     |
| Method   | POST                                      |
| Endpoint | /api/auth/email/verification-code/confirm |

**Request**

```json
{
  "email": "string",
  "code": "string"
}
```

**Response 성공**

```json
{
  "verified": true
}
```

**Response 실패**

```json
{
  "message": "인증 코드가 올바르지 않습니다."
}
```

### 3) 닉네임 중복 확인

| 항목     | 내용                                          |
| -------- | --------------------------------------------- |
| API 목적 | 닉네임 중복 여부 확인                         |
| Method   | GET                                           |
| Endpoint | /api/users/check-nickname?nickname={nickname} |

**Response 성공**

```json
{
  "available": true
}
```

**Response 실패**

```json
{
  "message": "이미 사용 중인 닉네임입니다."
}
```

### 4) 회원가입

| 항목     | 내용             |
| -------- | ---------------- |
| API 목적 | 사용자 회원가입  |
| Method   | POST             |
| Endpoint | /api/auth/signup |

**Request**

```json
{
  "email": "string",
  "password": "string",
  "password중복확인": "string",
  "nickname": "string",
  "agreements": {
    "terms-of-service": "1.0.0",
    "privacy-consent": "1.0.0",
    "age-confirm": "1.0.0",
    "marketing-consent": "1.0.0"
  }
}
```

- `agreements`: 회원가입 직전 매니페스트(§8-5)에서 받은 각 약관의 version을 그대로 담아 보냄.
- 필수 약관(`required: true`)의 키는 모두 포함되어야 함. 누락 시 서버는 400 반환.
- 선택 약관은 동의했을 때만 키를 포함 (또는 `null`로 명시). 미동의면 키 생략.
- 서버는 이 매핑을 회원의 동의 이력에 그대로 영구 저장하여, 추후 약관 개정 시 재동의 대상 판단·법적 증명에 활용.

**Response 성공**

```json
{
  "email": "string",
  "nickname": "string"
}
```

**Response 실패**

```json
{
  "message": "이미 가입된 이메일입니다."
}
```

추가 실패 케이스:

```json
{ "message": "필수 약관에 동의해야 회원가입이 가능합니다." }
```

```json
{ "message": "약관 버전이 최신이 아닙니다. 새로고침 후 다시 시도해주세요." }
```

### 5) 약관 매니페스트 조회

| 항목     | 내용                                    |
| -------- | --------------------------------------- |
| API 목적 | 회원가입 시 노출할 약관 메타데이터 조회 |
| Method   | GET                                     |
| Endpoint | /api/legal/manifest                     |

회원가입 페이지 진입 시 호출. 페이로드가 작아 페이지 첫 paint 차단 없음. 각 약관의 **현재 버전**·**필수 여부**·**본문 존재 여부** 등을 제공해 클라가 동의 체크박스 영역을 렌더하고, 사용자가 "보기"를 눌렀을 때 §8-6 본문 API를 호출하면 됨.

**Request** — 없음

**Response 성공**

```json
{
  "items": [
    {
      "key": "terms-of-service",
      "version": "1.0.0",
      "title": "서비스 이용약관",
      "required": true,
      "hasContent": true,
      "effectiveDate": "2026-06-01"
    },
    {
      "key": "privacy-consent",
      "version": "1.0.0",
      "title": "개인정보 수집·이용",
      "required": true,
      "hasContent": true,
      "effectiveDate": "2026-06-01"
    },
    {
      "key": "age-confirm",
      "version": "1.0.0",
      "title": "만 14세 이상입니다",
      "required": true,
      "hasContent": false
    },
    {
      "key": "marketing-consent",
      "version": "1.0.0",
      "title": "마케팅 정보 수신 동의",
      "required": false,
      "hasContent": true,
      "effectiveDate": "2026-06-01"
    }
  ]
}
```

필드 설명:

| 필드            | 타입                | 설명                                                                    |
| --------------- | ------------------- | ----------------------------------------------------------------------- |
| `key`           | string              | 약관 식별자. 본문 API path에 그대로 사용                                |
| `version`       | string (semver)     | 현재 게시 버전. 회원가입 시 `agreements` 매핑에 함께 전송               |
| `title`         | string              | 체크박스 옆에 노출할 제목                                               |
| `required`      | boolean             | true면 동의 안 하면 가입 불가                                           |
| `hasContent`    | boolean             | false면 "보기" 버튼 숨기고 본문 API 호출도 하지 않음 (예: 만 14세 확인) |
| `effectiveDate` | string (YYYY-MM-DD) | 시행일. 본문이 있는 약관에만 의미 있음                                  |

### 6) 약관 본문 조회

| 항목     | 내용                                 |
| -------- | ------------------------------------ |
| API 목적 | 특정 버전의 약관 본문(markdown) 조회 |
| Method   | GET                                  |
| Endpoint | /api/legal/{key}/{version}           |

사용자가 "보기" 버튼을 누른 시점에 lazy 호출. URL에 version이 포함되어 사실상 불변 리소스 — 강한 캐시 권장 (`Cache-Control: public, max-age=31536000, immutable`). 클라이언트도 `legal:{key}:{version}` 키로 device storage에 저장하여 재방문 시 네트워크 안 타게 함.

**Path 파라미터**

- `key`: 약관 식별자 (§8-5 응답의 `key`)
- `version`: 매니페스트에서 받은 정확한 version

**Response 성공** — `Content-Type: text/markdown; charset=utf-8`
원문 markdown 그대로. 클라가 markdown 렌더러로 표시.

**Response 실패**

```json
{ "message": "해당 약관 버전을 찾을 수 없습니다." }
```

### 약관 처리 흐름 정리

1. 회원가입 페이지 진입 → `GET /api/legal/manifest` 1회 호출
2. 매니페스트의 `items`를 순회하여 체크박스 영역 렌더 (제목·필수표시·"보기" 버튼)
3. 사용자가 "보기" 클릭 → `GET /api/legal/{key}/{version}` lazy 호출 → 클라 캐시
4. 사용자가 회원가입 버튼 클릭 → §8-4 회원가입 API의 `agreements`에 매니페스트의 version 그대로 매핑하여 전송
5. (재방문/재로그인 시 추후) — 매니페스트 비교로 사용자의 마지막 동의 버전과 다른 약관이 있으면 재동의 화면

## 9. 에러 처리

| 상황                         | 메시지                                                                           |
| ---------------------------- | -------------------------------------------------------------------------------- |
| 이메일 형식 오류 - 클라      | 올바른 이메일 형식을 입력해주세요.                                               |
| 이메일 미인증 - 클라         | 이메일 인증을 완료해주세요.                                                      |
| 인증 코드 미입력 - 클라      | 이메일에 발송된 인증 코드를 입력해주세요.                                        |
| 인증 코드 불일치 - 백        | 인증 코드가 올바르지 않습니다.                                                   |
| 인증 코드 만료 - 백          | 인증 코드가 만료되었습니다. 다시 인증해주세요.                                   |
| 인증 코드 재전송 제한 - 클라 | 잠시 후 다시 시도해주세요.                                                       |
| 비밀번호 조건 미충족 - 클라  | 비밀번호는 영문 대문자, 소문자, 숫자, 특수문자를 포함하여 8자 이상 입력해주세요. |
| 비밀번호 불일치 - 클라       | 비밀번호가 일치하지 않습니다.                                                    |
| 닉네임 조건 미충족 - 클라    | 닉네임은 특수문자 없이 2자 이상 12자 이하로 입력해주세요.                        |
| 닉네임 중복 미확인 - 클라    | 닉네임 중복 확인을 진행해주세요.                                                 |
| 이메일 중복 - 백             | 이미 가입된 이메일입니다.                                                        |
| 닉네임 중복 - 백             | 이미 사용 중인 닉네임입니다.                                                     |
| 네트워크 오류 - 백           | 네트워크 오류가 발생했습니다. 다시 시도해주세요.                                 |

## 10. 권한 및 보안

| 항목                  | 내용                                                                               |
| --------------------- | ---------------------------------------------------------------------------------- |
| 인증 방식             | 약관 동의, 이메일 인증 완료, 비밀번호 중복 확인, 닉네임 중복 확인 후 회원가입 가능 |
| 비밀번호 저장         | 서버에서 해시 처리                                                                 |
| 비밀번호 정책         | 영문 대문자, 소문자, 숫자, 특수문자 포함 / 최소 8자 이상                           |
| 이메일 중복 검사      | 서버에서 검증                                                                      |
| 닉네임 중복 검사      | 서버에서 검증                                                                      |
| 인증 코드 유효시간    | 3분                                                                                |
| 인증 코드 재전송 제한 | 10초                                                                               |
| 보호 정책             | 인증 코드 검증은 서버 기준으로 처리                                                |

## 11. 추후 확장 고려

- 이메일 인증 실패 횟수 제한
- 인증 코드 재발송 횟수 제한
- 핸드폰 번호 인증
- 소셜로그인 후 플로우 추가 고려
- pro 계정 고려
